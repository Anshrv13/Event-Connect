version: '3.8'

services:
  client:
    build:
      context: .
      dockerfile: client/Dockerfile.client
    ports:
      - "80:80"
    depends_on:
      - server

  server:
    build:
      context: .
      dockerfile: server/Dockerfile.server
    ports:
      - "5000:5000"
